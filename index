  <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>QUKANET Speed Test</title>
  <style>
    body { background: #000; color: #5AC8FA; font-family: sans-serif; margin:0; padding:0 20px;
           display:flex; flex-direction:column; align-items:center; justify-content:center; height:100vh; text-align:center;}
    header { font-size:2.5rem; margin-bottom:40px; font-weight:700; }
    .speed-container { display:flex; gap:80px; margin-bottom:30px; }
    .speed-box { background:#121212; padding:30px 40px; border-radius:12px; width:180px;
                 box-shadow:0 0 10px #0A84FF; }
    .speed-value { font-size:3.5rem; font-weight:800; }
    .speed-label { font-size:1.2rem; margin-top:8px; color:#3399FF; }
    .ping { font-size:1.3rem; margin-bottom:40px; color:#3399FF; }
    .test-options { margin-bottom:30px; font-size:1.1rem; }
    .test-options label { margin-right:25px; cursor:pointer; }
    .test-options input { width:18px; height:18px; vertical-align:middle; margin-right:6px; }
    .controls { display:flex; gap:20px; align-items:center; margin-bottom:40px; }
    button { background:rgba(90,200,250,0.2); border:2px solid #5AC8FA; color:#5AC8FA;
             padding:14px 32px; border-radius:8px; font-size:1.1rem; font-weight:700; cursor:pointer;
             transition:0.3s; }
    button:hover { background:#0A84FF; color:#fff; }
    select { background:#121212; color:#5AC8FA; border:1px solid #3399FF; border-radius:8px;
             padding:12px 20px; font-size:1rem; }
    .progress-bar { width:300px; height:10px; background:#121212; border-radius:5px;
                    overflow:hidden; margin-bottom:40px; box-shadow:0 0 10px #0A84FF; }
    .progress { height:100%; width:0%; background:#0A84FF; transition:0.3s; }
    footer { color:#3A3A3A; font-size:0.9rem; }
  </style>
</head>
<body>

<header>QUKANET</header>

<div class="speed-container">
  <div class="speed-box"><div class="speed-value" id="download-speed">0.00</div><div class="speed-label">Download Mbps</div></div>
  <div class="speed-box"><div class="speed-value" id="upload-speed">0.00</div><div class="speed-label">Upload Mbps</div></div>
</div>

<div class="ping">Ping: <span id="ping-value">0</span> ms</div>

<div class="test-options">
  <label><input type="checkbox" id="chk-download" checked /> Download Speed</label>
  <label><input type="checkbox" id="chk-upload" checked /> Upload Speed</label>
  <label><input type="checkbox" id="chk-ping" checked /> Ping (Latency)</label>
</div>

<div class="progress-bar"><div class="progress" id="progress-bar"></div></div>

<div class="controls">
  <button id="start-stop-btn">Start Test</button>
  <select id="test-duration">
    <option value="10">10 Seconds</option>
    <option value="15">15 Seconds</option>
    <option value="25">25 Seconds</option>
    <option value="30" selected>30 Seconds</option>
  </select>
</div>
